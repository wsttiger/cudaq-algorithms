[build-system]
requires = ["setuptools>=77", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "cudaq-algorithms-cu13"
dynamic = ["version"]
description = "Reusable quantum algorithm primitives built on CUDA-Q"
authors = [{name = "NVIDIA Corporation & Affiliates"}]
maintainers = [{name = "NVIDIA Corporation & Affiliates"}]
requires-python = ">=3.11"
readme = "README.md"
license = "Apache-2.0"
dependencies = [
  # The cudaq runtime ships Linux-only wheels; gate it so the pure
  # algorithms wheel still resolves on other platforms (classical
  # modules work there, circuit modules raise at point of use).
  "cuda-quantum-cu13 == 0.14.*; sys_platform == 'linux'",
  "numpy",
  "scipy",
]

[tool.setuptools]
package-dir = {"" = "python"}

[tool.setuptools.packages.find]
where = ["python"]
include = ["cudaq_algorithms*"]

[tool.setuptools_scm]
